spring:
  application:
    name: demo

  datasource:
    # Supabase connection uses standard postgresql driver
    url: jdbc:postgresql://${DB_HOST:aws-0-ap-northeast-1.pooler.supabase.com}:${DB_PORT:5432}/${DB_NAME:postgres}
    username: postgres.fnlpfcleuawvtkpleyzp
    password: 15903Kaitou
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  mail:
    host: ${SMTP_HOST:smtp-relay.brevo.com}
    port: ${SMTP_PORT:2525}
    username: ${SMTP_USERNAME:b7c107001@smtp-brevo.com}
    password: ${SMTP_PASSWORD:nhap_mat_khau_brevo_cua_ban_vao_day}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

admin:
  username: ${ADMIN_USER:admin}
  password: ${ADMIN_PASS:admin123}
